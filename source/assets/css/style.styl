//
// ooout: ../css/style.css, sourcemap: false, compress: true

oss ='./images/';
vs = '';

bgcolor = #000;
loadingColor = #fff;

vendor(prop, args)
    -webkit-{prop} args
    {prop} args

@import "reset";
@import "font";

.container {
    position absolute
    top 0
    left: 0
    width: 100%
    height: 100%
    overflow: hidden;
}